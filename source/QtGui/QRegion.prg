//
// Qt5xHb - Bindings libraries for Harbour/xHarbour and Qt Framework 5
//
// Copyright (c) 2026 Marcos Antonio Gambeta <marcosgambeta@outlook.com>
//

// DO NOT EDIT THIS FILE - the content was created using a source code generator

// clang-format off

#include <hbclass.ch>

#ifndef QT5XHB_NO_REQUESTS
REQUEST QRect
#endif

CLASS QRegion

   DATA pointer
   DATA self_destruction INIT .F.

   METHOD new
   METHOD delete
   METHOD swap
   METHOD isEmpty
   METHOD isNull
   METHOD contains
   METHOD translate
   METHOD translated
   METHOD united
   METHOD subtracted
   METHOD xored
   METHOD intersects
   METHOD boundingRect
   METHOD rectCount

   METHOD newFrom
   METHOD newFromObject
   METHOD newFromPointer
   METHOD selfDestruction
   METHOD setSelfDestruction

   DESTRUCTOR destroyObject

ENDCLASS

PROCEDURE destroyObject() CLASS QRegion
   IF ::self_destruction
      ::delete()
   ENDIF
RETURN

// clang-format on

#pragma BEGINDUMP

#include <QtCore/Qt>

#ifndef __XHARBOUR__
#include <QtGui/QRegion>
#endif

#include "qt5xhb_common.hpp"
#include "qt5xhb_macros.hpp"
#include "qt5xhb_utils.hpp"

#ifdef __XHARBOUR__
#include <QtGui/QRegion>
#endif

#define GET_PTR_FROM_SELF(p) auto p = static_cast<QRegion *>(Qt5xHb::itemGetPtrStackSelfItem())

HB_FUNC_STATIC(QREGION_NEW)
{
  if (ISNUMPAR(0)) {
    // QRegion()
    auto obj = new QRegion();
    Qt5xHb::returnNewObject(obj, true);
  } else if (ISBETWEEN(4, 5) && HB_ISNUM(1) && HB_ISNUM(2) && HB_ISNUM(3) && HB_ISNUM(4) && ISNUMORNIL(5)) {
    // QRegion(int x, int y, int w, int h, QRegion::RegionType t = QRegion::Rectangle)
    auto obj =
        new QRegion(PINT(1), PINT(2), PINT(3), PINT(4), HB_ISNIL(5) ? QRegion::Rectangle : PQREGION_REGIONTYPE(5));
    Qt5xHb::returnNewObject(obj, true);
  } else if (ISBETWEEN(1, 2) && ISQRECT(1) && ISNUMORNIL(2)) {
    // QRegion(const QRect &r, QRegion::RegionType t = QRegion::Rectangle)
    auto obj = new QRegion(*PQRECT(1), HB_ISNIL(2) ? QRegion::Rectangle : PQREGION_REGIONTYPE(2));
    Qt5xHb::returnNewObject(obj, true);
  } else if (ISBETWEEN(1, 2) && ISQPOLYGON(1) && ISNUMORNIL(2)) {
    // QRegion(const QPolygon &pa, Qt::FillRule fillRule = Qt::OddEvenFill)
    auto obj = new QRegion(*PQPOLYGON(1), HB_ISNIL(2) ? Qt::OddEvenFill : PQT_FILLRULE(2));
    Qt5xHb::returnNewObject(obj, true);
  } else if (ISNUMPAR(1) && ISQREGION(1)) {
    // QRegion(const QRegion &region)
    auto obj = new QRegion(*PQREGION(1));
    Qt5xHb::returnNewObject(obj, true);
  } else if (ISNUMPAR(1) && ISQBITMAP(1)) {
    // QRegion(const QBitmap &bitmap)
    auto obj = new QRegion(*PQBITMAP(1));
    Qt5xHb::returnNewObject(obj, true);
  } else {
    THROW_ERROR_3012();
  }
}

HB_FUNC_STATIC(QREGION_DELETE)
{
  GET_PTR_FROM_SELF(obj);
  DELETE_OBJECT(obj);
  RETURN_SELF();
}

// void swap(QRegion &other)
HB_FUNC_STATIC(QREGION_SWAP)
{
  GET_PTR_FROM_SELF(obj);

  if (obj != nullptr) {
#ifndef QT5XHB_DONT_CHECK_PARAMETERS
    if (ISNUMPAR(1) && ISQREGION(1)) {
#endif
      obj->swap(*PQREGION(1));
#ifndef QT5XHB_DONT_CHECK_PARAMETERS
    } else {
      THROW_ERROR_3012();
    }
#endif
  }

  RETURN_SELF();
}

// bool isEmpty() const
HB_FUNC_STATIC(QREGION_ISEMPTY)
{
  GET_PTR_FROM_SELF(obj);

  if (obj != nullptr) {
#ifndef QT5XHB_DONT_CHECK_PARAMETERS
    if (ISNUMPAR(0)) {
#endif
      RBOOL(obj->isEmpty());
#ifndef QT5XHB_DONT_CHECK_PARAMETERS
    } else {
      THROW_ERROR_3012();
    }
#endif
  }
}

// bool isNull() const
HB_FUNC_STATIC(QREGION_ISNULL)
{
  GET_PTR_FROM_SELF(obj);

  if (obj != nullptr) {
#ifndef QT5XHB_DONT_CHECK_PARAMETERS
    if (ISNUMPAR(0)) {
#endif
      RBOOL(obj->isNull());
#ifndef QT5XHB_DONT_CHECK_PARAMETERS
    } else {
      THROW_ERROR_3012();
    }
#endif
  }
}

HB_FUNC_STATIC(QREGION_CONTAINS)
{
  if (ISNUMPAR(1) && ISQPOINT(1)) {
    // bool contains(const QPoint &p) const
    GET_PTR_FROM_SELF(obj);

    if (obj != nullptr) {
      RBOOL(obj->contains(*PQPOINT(1)));
    }
  } else if (ISNUMPAR(1) && ISQRECT(1)) {
    // bool contains(const QRect &r) const
    GET_PTR_FROM_SELF(obj);

    if (obj != nullptr) {
      RBOOL(obj->contains(*PQRECT(1)));
    }
  } else {
    THROW_ERROR_3012();
  }
}

HB_FUNC_STATIC(QREGION_TRANSLATE)
{
  if (ISNUMPAR(2) && HB_ISNUM(1) && HB_ISNUM(2)) {
    // void translate(int dx, int dy)
    GET_PTR_FROM_SELF(obj);

    if (obj != nullptr) {
      obj->translate(PINT(1), PINT(2));
    }

    RETURN_SELF();
  } else if (ISNUMPAR(1) && ISQPOINT(1)) {
    // void translate(const QPoint &p)
    GET_PTR_FROM_SELF(obj);

    if (obj != nullptr) {
      obj->translate(*PQPOINT(1));
    }

    RETURN_SELF();
  } else {
    THROW_ERROR_3012();
  }
}

HB_FUNC_STATIC(QREGION_TRANSLATED)
{
  if (ISNUMPAR(2) && HB_ISNUM(1) && HB_ISNUM(2)) {
    // QRegion translated(int dx, int dy) const
    GET_PTR_FROM_SELF(obj);

    if (obj != nullptr) {
      RQREGION(obj->translated(PINT(1), PINT(2)));
    }
  } else if (ISNUMPAR(1) && ISQPOINT(1)) {
    // QRegion translated(const QPoint &p) const
    GET_PTR_FROM_SELF(obj);

    if (obj != nullptr) {
      RQREGION(obj->translated(*PQPOINT(1)));
    }
  } else {
    THROW_ERROR_3012();
  }
}

HB_FUNC_STATIC(QREGION_UNITED)
{
  if (ISNUMPAR(1) && ISQREGION(1)) {
    // QRegion united(const QRegion &r) const
    GET_PTR_FROM_SELF(obj);

    if (obj != nullptr) {
      RQREGION(obj->united(*PQREGION(1)));
    }
  } else if (ISNUMPAR(1) && ISQRECT(1)) {
    // QRegion united(const QRect &r) const
    GET_PTR_FROM_SELF(obj);

    if (obj != nullptr) {
      RQREGION(obj->united(*PQRECT(1)));
    }
  } else {
    THROW_ERROR_3012();
  }
}

// QRegion subtracted(const QRegion &r) const
HB_FUNC_STATIC(QREGION_SUBTRACTED)
{
  GET_PTR_FROM_SELF(obj);

  if (obj != nullptr) {
#ifndef QT5XHB_DONT_CHECK_PARAMETERS
    if (ISNUMPAR(1) && ISQREGION(1)) {
#endif
      RQREGION(obj->subtracted(*PQREGION(1)));
#ifndef QT5XHB_DONT_CHECK_PARAMETERS
    } else {
      THROW_ERROR_3012();
    }
#endif
  }
}

// QRegion xored(const QRegion &r) const
HB_FUNC_STATIC(QREGION_XORED)
{
  GET_PTR_FROM_SELF(obj);

  if (obj != nullptr) {
#ifndef QT5XHB_DONT_CHECK_PARAMETERS
    if (ISNUMPAR(1) && ISQREGION(1)) {
#endif
      RQREGION(obj->xored(*PQREGION(1)));
#ifndef QT5XHB_DONT_CHECK_PARAMETERS
    } else {
      THROW_ERROR_3012();
    }
#endif
  }
}

HB_FUNC_STATIC(QREGION_INTERSECTS)
{
  if (ISNUMPAR(1) && ISQREGION(1)) {
    // bool intersects(const QRegion &r) const
    GET_PTR_FROM_SELF(obj);

    if (obj != nullptr) {
      RBOOL(obj->intersects(*PQREGION(1)));
    }
  } else if (ISNUMPAR(1) && ISQRECT(1)) {
    // bool intersects(const QRect &r) const
    GET_PTR_FROM_SELF(obj);

    if (obj != nullptr) {
      RBOOL(obj->intersects(*PQRECT(1)));
    }
  } else {
    THROW_ERROR_3012();
  }
}

// QRect boundingRect() const
HB_FUNC_STATIC(QREGION_BOUNDINGRECT)
{
  GET_PTR_FROM_SELF(obj);

  if (obj != nullptr) {
#ifndef QT5XHB_DONT_CHECK_PARAMETERS
    if (ISNUMPAR(0)) {
#endif
      RQRECT(obj->boundingRect());
#ifndef QT5XHB_DONT_CHECK_PARAMETERS
    } else {
      THROW_ERROR_3012();
    }
#endif
  }
}

// int rectCount() const
HB_FUNC_STATIC(QREGION_RECTCOUNT)
{
  GET_PTR_FROM_SELF(obj);

  if (obj != nullptr) {
#ifndef QT5XHB_DONT_CHECK_PARAMETERS
    if (ISNUMPAR(0)) {
#endif
      RINT(obj->rectCount());
#ifndef QT5XHB_DONT_CHECK_PARAMETERS
    } else {
      THROW_ERROR_3012();
    }
#endif
  }
}

HB_FUNC_STATIC(QREGION_NEWFROM)
{
  NEWFROM();
}

HB_FUNC_STATIC(QREGION_NEWFROMOBJECT)
{
  HB_FUNC_EXEC(QREGION_NEWFROM);
}

HB_FUNC_STATIC(QREGION_NEWFROMPOINTER)
{
  HB_FUNC_EXEC(QREGION_NEWFROM);
}

HB_FUNC_STATIC(QREGION_SELFDESTRUCTION)
{
  SELFDESTRUCTION();
}

HB_FUNC_STATIC(QREGION_SETSELFDESTRUCTION)
{
  SETSELFDESTRUCTION();
}

#pragma ENDDUMP
